# Public app metadata (safe to expose to the browser)
NEXT_PUBLIC_APP_NAME="سامانه مدیریت یکپارچه آروین"

# Base URL of the backend API this dashboard will eventually consume.
# The prototype currently runs entirely on mock data in lib/data/*.
NEXT_PUBLIC_API_BASE_URL="https://api.example.com"
